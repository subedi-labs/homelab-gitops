apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn
  namespace: authentik
spec:
  parentRefs:
    - name: shared-gateway
      namespace: gateway-system
  hostnames:
    - longhorn.pukarsubedi.com
  rules:
    - backendRefs:
        - name: authentik-server
          port: 80
