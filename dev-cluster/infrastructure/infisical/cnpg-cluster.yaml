apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: infisical-db
  namespace: infisical
spec:
  instances: 1
  storage:
    storageClass: longhorn
    size: 2Gi
  bootstrap:
    initdb:
      database: infisical
      owner: infisical
